# ═══════════════════════════════════════════════════════════
# VLE Backend Environment Configuration
# ═══════════════════════════════════════════════════════════
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ═══════════════════════════════════════════════════════════

# ─── Server Configuration ─────────────────────────────────
PORT=3001

# ─── Database Configuration ───────────────────────────────
# Use the AWS RDS database (shared across all machines)
DB_HOST=vle-postgres-db1.cjc8wogm48c3.ap-south-1.rds.amazonaws.com
DB_PORT=5432
DB_USER=vle_admin
DB_PASSWORD=vle20022026
DB_NAME=vle_db
NODE_ENV=sandbox

# ─── JWT Authentication ───────────────────────────────────
JWT_SECRET=dev_secret_key_change_in_production
JWT_EXPIRES_IN=1h

# ─── AWS Configuration ────────────────────────────────────
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=AKIA5Z7Q3HLYOXJLGKQ3
AWS_SECRET_ACCESS_KEY=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

# ─── AWS S3 (for file uploads) ────────────────────────────
# S3 is used to store user skill verification submissions
# 
# Current: USE_S3=false (submissions stored in PostgreSQL)
# Production: Set USE_S3=true to store in S3 bucket
# 
# When USE_S3=false: Submissions stored directly in database (simpler, good for dev)
# When USE_S3=true: Submissions uploaded to S3 (scalable, good for production)
# 
# See AWS_S3_USAGE.md for detailed explanation
S3_BUCKET_NAME=vleu

# ─── AWS Lambda (for code grading) ────────────────────────
USE_LAMBDA=true
LAMBDA_FUNCTION_NAME=vle-code-grader

# ─── CORS Configuration ───────────────────────────────────
FRONTEND_URL=http://localhost:3000
